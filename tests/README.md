# Test Scripts and Data

This directory contains test scripts and temporary data files used during development.

## Structure

- `scripts/` - Test scripts for debugging and development
- `data/` - Temporary JSON files (card exports, etc.)

## Note

These files are ignored by git (see `.gitignore`). They are for local development only.
